#include<stdio.h>#define inf 100001int min (int a, int b);int main () {        int d[101][101] = {inf};        int city,ways,i,a,b,k,l,j;    int array[101][101];    for(int i = 1; i <= 101; ++i)    {        for (int j = 1; j <= 101 ; ++j){            array[i][j] = inf;        }            }    scanf("%d %d", &city, &ways);        for (i=0; i<ways ; i++) {        scanf("%d %d %d", &a,&b,&k);        array[a][b] = k;    }        for (l=1; l<city ; l++)        for (i=1; i<city ; i++)            for (j=1; j<city ; j++)                if (array[i][l] != inf &&                    array[l][j] != inf &&                    array[i][l] + array[l][j] < array[i][j])                    array[i][j] = array[i][l] + array[l][j];               for (int i = 1; i <= city; i++)        for (int j = 1; j <= city; j++)            if (array[i][j] == inf)                printf("%7s", "INF");            else                printf ("%7d", array[i][j]);        printf("\n");    }int min (int a, int b){        if (a >= b) {return b;}    else        {return a;}    }