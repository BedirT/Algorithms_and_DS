// Mult.

#include <stdio.h>
#include <string.h>

int num1[1001], num2[1001];
int mult[1005];

int main (){
    
    char str1[1001], str2[1001];
    int len1 = 0, len2 = 0, lenT;
    int i, j;
    
    scanf("%s %s", str1, str2);
    for (i = strlen(str1) ; i >= 0 ; --i)
        num1[len1++] = (int)(a[i] - '0');
        
        
}
