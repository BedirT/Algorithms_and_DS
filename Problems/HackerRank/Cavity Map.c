#include <stdio.h>#include <math.h>#include <stdlib.h>int grid[100][100];int main(){    int n,i,j,num;    scanf("%d",&n);        for (i=0; i<n; i++){        int l=n-1;        scanf("%d",&num);        for (j=0; j<n; j++){            grid[i][j]=num/pow(10,l);            grid[i][j]%=10;            l--;        }    }        for (i=0; i<n; i++) {        for (j=0; j<n; j++) {            if (i!=0 && i!=n-1 && j!=0 && j!=n-1 && grid[i][j]>grid[i-1][j] && grid[i][j]>grid[i][j-1] && grid[i][j]>grid[i][j+1] && grid[i][j]>grid[i+1][j]) {                printf("X");            }else                printf("%d",grid[i][j]);                    }        printf("\n");    }        return 0;}