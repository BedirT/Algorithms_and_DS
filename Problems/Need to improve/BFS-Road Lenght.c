// This code is not working for the question// It is because of the array size, 2D , n(n-1)/2// Overflowing...#include <stdio.h>#include <math.h>#include <stdbool.h>int queue[1001];int rear = 0;int front = 0;void insert(int num){        if (front == - 1)        front = 0;    rear += 1;    queue[rear] = num;    }int delete(){    front += 1;    return queue[front];}int t, n, m, a, b, s, i, j;int array[1001][1001];bool was[1001];int d[1001];int main(){        scanf("%d", &t);        while(t--){                scanf(" %d %d", &n, &m);                for(i=1; i<=m; i++){            scanf(" %d %d", &a, &b);            array[a][b] = 6;        }        scanf(" %d", &s);        for(i=1; i<=n; i++){            for(j=1; j<=n; j++){                //printf("%d %d\n",s,j);                if(array[s][j] == 6 && !was[j]  ){                    was[j] = true;                    d[j] = d[s] + 6;                    //printf("%d %d\n", d[s], j);                    insert(j);                }            }s = delete();                    }// End of for loop        for(i=2 ; i<=n ; i++){            if(d[i]==0)                printf("-1 ");            else                printf("%d ", d[i]);        }printf("\n");        for(i=1; i<=n; i++){            for(j=1; j<=n; j++){                array[i][j]=0;                d[i]=0;                was[i]=false;                queue[i]=0;            }}            }// End of while}// End of main